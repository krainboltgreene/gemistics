require "spec_helper"

describe Gemistics::Request::Gems do
  let(:gems) { described_class.new }
  describe "#names" do
    context "with already stored data" do

    end

    context "without stored data" do

    end
  end
end
